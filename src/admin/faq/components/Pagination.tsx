export function Pagination({page,totalPages,onChange}:{page:number;totalPages:number;onChange:(page:number)=>void}){if(totalPages<=1)return null;const s=Math.floor(page/10)*10,e=Math.min(s+10,totalPages);return <div className="pagination"><button disabled={s===0} onClick={()=>onChange(Math.max(s-10,0))} type="button">이전</button>{Array.from({length:e-s},(_,i)=>s+i).map(i=><button className={page===i?'pagination__active':''} key={i} onClick={()=>onChange(i)} type="button">{i+1}</button>)}<button disabled={e===totalPages} onClick={()=>onChange(Math.min(s+10,totalPages-1))} type="button">다음</button></div>}
